name: build_the_app

on:
  push:
    branches:
      - main 

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build the Splunk app
        uses: VatsalJagani/splunk-app-action@v4
        with:
          app_dir: "package"
          is_app_inspect_check: false